version: "3.5"
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    network_mode: host
    restart: always
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    network_mode: host
    restart: always